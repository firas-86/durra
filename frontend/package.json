{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
  "build": "tsc && cd frontend && npm install && npm run build",
  "start": "node dist/server.js"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "typescript": "^5.3.0"
  }
}
